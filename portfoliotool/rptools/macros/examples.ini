[DEFAULT]
group = "EXAMPLES"
colorKey = "yellow"
fontColorKey = "black"

[cmd]
test = "{{ key }}"
command = "{{ obj }}"

[feats]
command = basic/feats.jinja2
group = "Feats"
label = "{{key}}"

[saves]
command = basic/saves.jinja2
group = "Saves"
label = "{{key|replace(' Save', '')}}"

[melee]
test = "{{ obj.typetext }} "
